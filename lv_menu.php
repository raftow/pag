<?php
     
      /*
      obsolete
      $module = AfwUrlManager::currentURIModule();       
        
      $numfrontclass = 0;
      $nummenu = 1;
      $numtheme = 0;
      $numsubtheme = 0;      
      $theme[$numtheme] = "تم العمل عليه منذ قليل";
      $subtheme[$numtheme][$numsubtheme] = "آخر الاجراءات المهمة";
      for($ss=0;$ss<4; $ss++) 
      {
                if($_SE SSION["LAST_VISITED"][$module][$ss]) 
                {
                      $lv_mpage = $_SE SSION["LAST_VISITED"][$module][$ss]["Main_Page"];
                      $lv_params = $_SESS ION["LAST_VISITED"][$module][$ss]["Params"];
                      $lv_title = $_SES SION["LAST_VISITED"][$module][$ss]["Title"];  
                      $menu[$numtheme][$numsubtheme][$nummenu++] = array("page"=>"main.php?Main_Page=${lv_mpage}&${lv_params}", "png"=>"../lib/images/profile.png", "titre"=>"$lv_title", "id"=>"", "class"=>"front_$numfrontclass", "subtheme"=>0);
                }
                $numfrontclass = ($numfrontclass + 1) % 15;
      }
      $numsubtheme++;
      $subtheme[$numtheme][$numsubtheme] = "اجراءات أخرى مهمة";
      for($ss=4;$ss<8; $ss++) 
      {
                if($_SES SION["LAST_VISITED"][$module][$ss]) 
                {
                      $lv_mpage = $_SES SION["LAST_VISITED"][$module][$ss]["Main_Page"];
                      $lv_params = $_SES SION["LAST_VISITED"][$module][$ss]["Params"];
                      $lv_title = $_SESS ION["LAST_VISITED"][$module][$ss]["Title"];  
                      $menu[$numtheme][$numsubtheme][$nummenu++] = array("page"=>"main.php?Main_Page=${lv_mpage}&${lv_params}", "png"=>"../lib/images/profile.png", "titre"=>"$lv_title", "id"=>"", "class"=>"front_$numfrontclass", "subtheme"=>0);
                }
                $numfrontclass = ($numfrontclass + 1) % 15;
      }
      
      include "../pag/menu_constructor.php";
      
       */
      
      


?>

